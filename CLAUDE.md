# CLAUDE.md — このプロジェクトでの Claude の振る舞い

## このリポジトリの目的

Udemy 講座「**React + TypeScript で超本格的な Notion クローンを作ろう**」を進めるための**学習用**ワークスペース。

- 本番運用や商用利用は想定していない
- 「写経して動かす」だけでなく、**コードの意図と設計判断を理解すること**が目的
- 業務では Claude Code を使った AI 駆動開発をしているため、「動くけど中身が分からない」状態を埋めたいニーズがある

## 技術スタック

| カテゴリ | 内容 |
| --- | --- |
| 言語 | TypeScript `~6.0.2` |
| ライブラリ | React 19 (`^19.2.5`) / React DOM (`^19.2.5`) |
| ルーティング | react-router-dom `^7.15.0` |
| HTTP クライアント | axios `^1.16.0` |
| UI 補助 | cmdk, react-icons |
| ビルドツール | Vite `^8.0.10` + `@vitejs/plugin-react` |
| Lint | ESLint `^10.2.1` + typescript-eslint / react-hooks / react-refresh |

バックエンド（API）は別リポジトリ `notion-clone-api/` にある（このリポジトリでは触らない）。

## ディレクトリ構成（要点のみ）

```
src/
├── App.tsx, Layout.tsx, main.tsx       # エントリポイント周り
├── components/                          # 再利用 UI コンポーネント
├── pages/                               # ルーティングごとのページ
├── lib/                                 # 横断的なユーティリティ（api クライアントなど）
├── modules/                             # ドメインごとの機能単位
│   ├── auth/                            # 認証（signup など）
│   └── users/                           # ユーザー (User クラス、エンティティ)
└── styles/                              # スタイル
docs/
└── learning-log/                        # 日次の学習ログ（YYYY-MM-DD.md）
```

## ユーザーの経験レベル（説明の調整に使う）

- TypeScript / React は初級〜中級の入口
- 初心者向け動画講座を複数こなし、React 公式ドキュメントのクイックスタートは完了済み
- `interface` / `type` の違い、基本的な型注釈は既知
- 業務では Claude Code を使った AI 駆動開発でフロントエンドを書いている

## Claude への指示

### 説明スタイル

- **「TS とは」「React とは」レベルの基礎説明は不要**
- ただし以下の領域はフレームワーク固有の慣習が絡むので**丁寧に**説明する:
    - クラスの初期化（`!` の意味、`Object.assign` パターン）
    - React のフック（`useState`, `useEffect`, `useContext` など）
    - React 19 の新機能（`use`, Server Components, Actions など）
    - axios のインターセプタや型の付け方
    - Vite / TypeScript の設定（`tsconfig.json`, `vite.config.ts`）
- **「なぜそう書くのか」の背景**を必ず添える（コンパイラの挙動、ライブラリの設計思想、慣習の歴史的経緯）
- **❌ ダメな例 / ✅ 良い例** の対比で示す
- **代替案も併記**して、なぜその書き方が選ばれているかが分かるようにする
- **コードの意図や設計判断を言語化する** — AI 駆動開発で抜けがちな部分を補強する

### 進め方

- 過剰に基礎から説明しすぎない（既知のことを長々と書かない）
- ユーザーが選択した（IDE で開いている / ハイライトしている）コード片に絞って質問してくることが多い
- 質問が抽象的なときは、「具体的にどの行が気になりますか？」と聞き返してよい

### 学習ログの運用

- 1 日の学習が終わったら `docs/learning-log/YYYY-MM-DD.md` に進捗・疑問点をまとめる
- 各ログに含める項目:
    1. 今日のテーマ（題材になったコード）
    2. 学んだこと（要点を箇条書き、コード例つき）
    3. 残った疑問・今後の学習ポイント
    4. 参考: 関連ファイルへのリンク
- ファイルへのリンクは Markdown のリンク記法 `[label](path)` を使う
- 過去の学習ログを参照して「前回はここまで進んだ」を踏まえた回答をする

### コードを書くときの注意

- 講座の写経が前提なので、**講座と異なる独自実装を勝手に増やさない**
- リファクタリングや「もっと良い書き方」の提案は、ユーザーが求めたときだけ
- ライブラリのバージョンは `package.json` に従う（README 参照）

## ローカル開発

```bash
npm install              # 依存関係のインストール
npm run dev              # 開発サーバー起動 (HMR)
npm run build            # 型チェック + プロダクションビルド
npm run lint             # ESLint 実行
npm run preview          # ビルド成果物のプレビュー
```

## 参考リンク

- [README.md](./README.md) — リポジトリの概要・セットアップ手順
- [docs/learning-log/](./docs/learning-log/) — 日次学習ログ
